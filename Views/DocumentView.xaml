<UserControl
    x:Class="NugetMVVP.Views.DocumentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NugetMVVP.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Style>
        <Style>
            <Style.Triggers>
                <EventTrigger RoutedEvent="UserControl.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                From="0.0"
                                To="1.0"
                                Duration="0:0:0.5" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Grid Background="#303030">
        <TextBlock
            Margin="5,12,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            FontFamily="Segoe UI"
            FontSize="24"
            Foreground="#DDDDDD"
            Text="Document" />

        <Grid
            Width="180"
            Height="40"
            Margin="0,10,15,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top">
            <TextBox
                x:Name="SearchBox"
                Style="{StaticResource SearchBoxStyle}"
                Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />


            <TextBlock Style="{StaticResource SearchTextStyle}" Text="Search" />
        </Grid>

        <ItemsControl
            Width="650"
            Height="300"
            Margin="0,0,0,0"
            HorizontalAlignment="Center"
            ItemsSource="{Binding DocumentSourceCollection}">

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <RadioButton
                        Content="{Binding DocumentName}"
                        GroupName="DocumentGroup"
                        Style="{StaticResource DocumentButtonStyle}"
                        Tag="{Binding DocumentImage}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="6" Rows="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

        </ItemsControl>
    </Grid>
</UserControl>
